"""
BiblioNet-Bidirectional Crawler (binet)

A self-contained tool that expands a citation network bidirectionally
(backward = references, forward = cited-by) starting from a seed set of DOIs.

See the requirement document (双向引文网络需求文档.md) for the full contract.
"""

__version__ = "1.0.0"
